class Task{
    constructor(titulo, descripcion, fecha_creacion, fecha_limite,estado){
        this.titulo = titulo;
        this.descripcion = descripcion;
        this.fecha_creacion = fecha_creacion;
        this.fecha_limite = fecha_limite;
        this.estado=estado
    }
}